{
    "name": "who-am-i",
    "version": "1.0.0",
    "scripts": {
        "develop:server": "npm run --prefix server start",
        "develop:client": "wait-on http://localhost:8081 && npm run --prefix client serve",
        "develop": "cross-env FORCE_COLOR=1 npm-run-all -l -p develop:*"
      },
      "devDependencies": {
        "npm-run-all": "4.1.5",
        "wait-on": "5.2.1",
        "cross-env": "7.0.3"
      }
  }
  